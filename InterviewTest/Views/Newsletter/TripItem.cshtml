@model InterviewTest.Controllers.NewsletterTripViewModel

<div class="item @Model.addclass">
    <div class="trip">
        <img src="@Model.ImageUrl" />
        <div>
            @Model.Name with @Model.HostName<br />
            @Model.Country
        </div>
    </div>
</div>